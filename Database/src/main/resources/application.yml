server:
  port: 8082

spring:
  application:
    name: Database
  datasource:
    url: jdbc:h2:mem:wookie_products
    username: wookie
    password: wookie_products
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: create
